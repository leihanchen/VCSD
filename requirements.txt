# VCSD training environment — core pinned dependencies.
#
# Install:
#   pip install --no-deps -r requirements.txt
#   # flash-attn and causal-conv1d ship compiled CUDA extensions tied to the torch
#   # build, so install them from source AFTER torch, forcing a rebuild:
#   pip install --no-build-isolation --force-reinstall --no-deps flash-attn
#   pip install --no-build-isolation --force-reinstall --no-deps causal-conv1d==1.6.1
#
# Verified stack: torch 2.10 / vllm 0.18 / transformers 5.5 / flash-attn 2.8.3.post1.

torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0
torchdata==0.11.0
vllm==0.18.0
transformers==5.5.0
flash-linear-attention==0.4.2
ray==2.53.0
tensordict==0.10.0
pyvers==0.1.0
cloudpickle==3.1.2
hydra-core==1.3.2
codetiming==1.4.0
cbor2==5.8.0
datasets==4.4.2
